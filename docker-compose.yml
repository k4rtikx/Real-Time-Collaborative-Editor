services:

  web:
    build: .
    container_name: django_editor

    ports:
      - "8000:8000"

    env_file:
      - .env

    depends_on:
      - redis

    volumes:
      - .:/app

  redis:
    image: redis:7
    container_name: redis_editor

    ports:
      - "6379:6379"